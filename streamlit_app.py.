import streamlit as st

st.set_page_config(page_title="Magic Pixel AI", page_icon="🚀")

st.title("🚀 Magic Pixel AI")
st.write("النسخة الاحترافية المستقرة - تم التفعيل بنجاح")

prompt = st.text_input("اكتبي وصف الصورة بالإنجليزية:", value="A beautiful landscape")

if st.button("توليد ✨"):
    if prompt:
        # سيرفر صور سريع جداً لا يعتمد على API معقد
        image_url = f"https://loremflickr.com/1024/1024/{prompt.replace(' ', ',')}"
        st.image(image_url, caption="Result", use_container_width=True)
        st.balloons()
